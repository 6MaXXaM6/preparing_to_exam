Сальников:
Пример взаимодействия паскаля с ассемблером.

всё содержательное находится в файле с модулем pascal (mod_init_pas.pas)
и в ассемблерном файле (mip_pas.asm).


; комментарий меня: скомпилить ассемблер потом скомпилить модуль (fpc mod_init_pas.pas) потом компилить print_rand_array fpc сам слинкует exe будет
